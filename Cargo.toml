[package]
name = "uefi_pi"
version = "0.1.0"
edition = "2024"

[dependencies]

[dev-dependencies]
clap = {version = "=4.6.0", features = ["derive"]}

[lib]
name = "uefi_fv"

[[example]]
name = "parse_efi_vol"